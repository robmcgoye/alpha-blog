<h1 class="text-center mt-3">Categories</h1>
<div class="flickr_pagination">
    <%= will_paginate @categories, :container => false %>
</div>
<%= render 'category', obj: @categories %>
<div class="flickr_pagination mb-4">
    <%= will_paginate @categories, :container => false %>
</div>